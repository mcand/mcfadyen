import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    margin: '20px 20px',
  },
  media: {
    height: 340,
  },
  link: {
    textDecoration: 'none',
  },
});